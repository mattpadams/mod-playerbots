"""v2 admin API routers.

Each module exports a ``router`` (``fastapi.APIRouter``) mounted under
``/api/v2`` by ``main.py``. Route handlers resolve services via the
module-level registry in ``deps.py``.
"""
